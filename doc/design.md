+ There are nine action slots. On controller, use the south button to target and use the W N E + bumpers to access all actions.
+ Make resources public / private / secret
* Allow CLI arguments to use descriptive names instead of ints
+ The follow action should change a strategy
+ MiniMap? It's probably worth doing
    https://github.com/eilamg/godot-tilemap-minimap/blob/master/Minimap.gd
- on_target action
+++ Game Interface
+++ Complete 1st set of actions / measures / resources
+++ Chat Bubbles
+++ BB Plates <--- marked up descriptive plates that open up to provide more 
detailed documents until the user dismissises
+++ Focus Group Management : There will be no concept of parties. Instead, each 
user must focus all of their related party members. Focused actors will bind to bumpers + dpads. Allowing up to 12. <-- Unsure how to set/clear that as of now. Probably a sub menu off the selet button.

Campaign Analyzer: <-- Before loading, a campaign Analyzer should lint and provide feedback in a log file.

name: Atlas Myth

++ Keybinds
++ On target signal
